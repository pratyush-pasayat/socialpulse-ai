aiohappyeyeballs==2.6.2
aiohttp==3.14.1
aiosignal==1.4.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.0
attrs==26.1.0
certifi==2026.6.17
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.1
cryptography==49.0.0
distro==1.9.0
fastapi==0.138.0
filelock==3.29.4
frozenlist==1.8.0
fsspec==2026.6.0
groq==1.5.0
h11==0.16.0
h2==4.3.0
hpack==4.2.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
hyperframe==6.1.0
idna==3.18
Jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
MarkupSafe==3.0.3
mdurl==0.1.2
multidict==6.7.1
numpy==2.4.6
packaging==26.0
postgrest==2.31.0
propcache==0.5.2
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
Pygments==2.20.0
PyJWT==2.13.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
PyYAML==6.0.3
realtime==2.31.0
referencing==0.37.0
regex==2026.5.9
requests==2.34.2
requests-oauthlib==2.0.0
rpds-py==2026.5.1
six==1.17.0
sniffio==1.3.1
starlette==1.3.1
storage3==2.31.0
StrEnum==0.4.15
supabase==2.31.0
supabase-auth==2.31.0
supabase-functions==2.31.0
tenacity==9.1.4
tqdm==4.68.3
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
uvicorn==0.49.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==15.0.1
yarl==1.24.2

# --- Removed (unused in this codebase) ---
# torch, transformers, sentence-transformers, chromadb, onnxruntime
#   -> not imported anywhere; likely leftover from an earlier experiment
# praw, prawcore
#   -> reddit_tool.py is currently empty (Reddit integration pending API approval)
# newsapi-python
#   -> news_tool.py uses raw `requests` calls instead, this package is unused
# scikit-learn, scipy, joblib, threadpoolctl, networkx, sympy, mpmath
#   -> only pulled in as sub-dependencies of torch/sentence-transformers
# tokenizers, safetensors, huggingface_hub, hf-xet
#   -> only pulled in as sub-dependencies of transformers
# chromadb, kubernetes, grpcio, opentelemetry-*, googleapis-common-protos,
# protobuf, mmh3, overrides, PyPika, durationpy, bcrypt, defusedxml,
# importlib_resources, build, pyproject_hooks, deprecation
#   -> only pulled in as sub-dependencies of chromadb
# typer, rich, shellingham
#   -> only pulled in as CLI tooling for chromadb, not used by this app
# scikit-learn's flatbuffers dependency
#   -> only needed by onnxruntime
#
# IMPORTANT: before deploying, run a fresh install in a clean venv/conda env
# and confirm the app starts and a real search works end-to-end:
#   pip install -r requirements.txt --break-system-packages  (or plain pip install in a fresh venv)
#   uvicorn main:app --reload --port 8000
# If anything's missing, pip will throw a clear ModuleNotFoundError naming
# exactly which package to add back.